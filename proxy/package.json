{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Proxy server for ngrok tunnel to inject ngrok-skip-browser-warning header",
  "main": "proxyServer.js",
  "scripts": {
    "start": "node proxyServer.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "ngrok",
    "ngrok-skip-browser-warning"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "http-proxy-middleware": "^3.0.5"
  }
}
